# MyAgent 架构设计说明

## 1. 设计目标与约束

- **本地优先**：对话与偏好默认驻留客户端；模型推理可发生在用户自管的云端 API 或本地 Ollama，由用户配置。
- **职责分离**：渲染进程负责交互与即时状态；主进程承载文件访问、HTTPS 出站、以及与操作系统相关能力（快捷键、托盘等，以实现对齐）。
- **可替换供应商**：文本模型、嵌入向量、网页搜索均采用可插拔的「提供商 + endpoint + model」模型。

## 2. 逻辑分层

```mermaid
flowchart TB
    subgraph Renderer["渲染进程 Chromium"]
      UI["React UI\nChatWindow / SessionList / SettingsPanel"]
      Store["Zustand Stores\nchat / model / knowledge / workspace / ..."]
      UI <--> Store
    end

    subgraph Preload["预加载 preload.cjs/ts"]
      Bridge["注入 window.electron API"]
    end

    subgraph Main["Electron 主进程 Node"]
      IPC["ipcMain handlers\nmodel / export / knowledge / web-search / ..."]
      UTIL["工具：embeddingClient、vectorIndexPersistence、documentText …"]
      IPC --> UTIL
    end

    Store -->|"invoke"| Bridge
    Bridge --> IPC
```

### 2.1 展示层（`src/`）

- **组件**：会话列表 `SessionList`、对话主区 `ChatWindow`、气泡 `MessageItem`、Markdown 渲染 `MarkdownContent`、设置 `SettingsPanel`、引导 `OnboardingSteps`。
- **状态**：会话 `chatStore`、模型列表 `modelStore`、知识与嵌入 `knowledgeStore`、工作区 `workspaceStore`、联网 `webSearchStore`、全局设置 `settingStore`。
- **国际化**：文案聚合于 `src/i18n/ui.ts`，通过 `useI18n()` 取用。

### 2.2 预加载与会话边界

- `preload` 暴露白名单 API（`callModel`、`knowledgeSearch`、`persistGet`/`persistSet`、`readWorkspaceHint`、`webSearch` 等），避免渲染进程直接使用任意 Node/Electron 模块（具体以 `preload.cjs` 与类型定义为准）。

### 2.3 主进程（`electron/`）

| 模块 | 职责概要 |
|------|----------|
| `ipc/model`、`ipc/model-stream` | HTTP(S) 请求大模型，非流式与 SSE 流式；错误归一与用户可见提示 |
| `ipc/export`、`ipc/file`、`ipc/documents` | 工作区枚举、会话导出路径、结构化文档读取与辅助导出 |
| `ipc/web-search` | 封装各搜索后端 |
| `ipc/knowledge` + `utils/workspaceIndex`、`knowledgeIndexOperations`、`vectorIndexPersistence`、`embeddingClient` | 建索引：**全文**或按文件指纹的**增量**（改动文件单独重嵌入、未改向量复用）→向量 JSON 落盘；查询：单次嵌入→余弦相似度→相关度截断 |
| `utils/documentText` | Excel / Word / Markdown 转为模型可读正文 |
| `utils/markdownExport` | 从助手 Markdown 管道表生成 xlsx/docx |

## 3. 关键数据流

### 3.1 发送一条用户消息（简化）

1. 用户在 `ChatWindow` 编辑文本与可选附件；
2. 渲染进程通过 `enrichMessagesForModel` 拉取附件解析结果并入消息列表快照；
3. 若启用工作区：`readWorkspaceHint`、可选 `knowledgeSearch`（在主进程读取索引与嵌入）；
4. 若满足联网触发条件：`webSearch`；
5. 合并后的 messages 发往 `subscribeModelStream` 或等价非流接口；
6. 流式增量回写会话 store，收尾后持久化。

### 3.2 向量知识库流水线

```mermaid
sequenceDiagram
  participant UI as 设置/ChatWindow
  participant IPC as knowledge IPC
  participant IDX as workspaceIndex
  participant EMB as embeddingClient
  participant FS as vectorIndexPersistence

  UI->>IPC: knowledgeIndexWorkspace(root, embed 配置)
  IPC->>IDX: collectWorkspaceChunks(root)
  IDX-->>IPC: 文本块列表
  loop 分批
    IPC->>EMB: fetchEmbeddingsBatched
    EMB-->>IPC: 向量矩阵
  end
  IPC->>FS: writeVectorIndex(含 dim、chunks)
  UI->>IPC: knowledgeSearch(query, ...)
  IPC->>FS: readVectorIndex
  IPC->>EMB: fetchQueryEmbedding
  IPC->>IPC: rankByCosine + selectChunkIndicesByRelevance + 字数截断
  IPC-->>UI: text + meta
```

## 4. 持久化与客户数据

- 会话与用户偏好：**Zustand + 自定义持久化**（`persistSet`/`persistGet`）；渲染进程对用户数据 JSON 写入带**防抖**，并在 `beforeunload` 冲刷，减小大会话场景的磁盘抖动窗口。
- 向量索引：`vector-index-v1.json`（见 `vectorIndexFilePath()`）含 `chunks` 与可选 **`fingerprints`**（相对 POSIX 路径 → `mtime` / 字节规模），用于增量建索引时对未改动文件向量复用；无指纹的旧文件在用户首次触发「增量」时会退化为**全文重建并写入指纹**。聊天历史与该文件分离存放。

## 5. Electron 生命周期与流式收尾

- **单实例**：`app.requestSingleInstanceLock()`，`second-instance` 时唤醒主窗，避免两套进程争用 `userData`。
- **首窗展示**：BrowserWindow `show: false`，`ready-to-show` 后再 `show()`。
- **`render-process-gone`**：在非干净退出时打日志便于分发版排查。
- **流式 preload**：用户对 `subscribeModelStream` 的返回值仅发送 `abort`；须在收到主进程 `model-stream-end`/`error` 后再移除 IPC 监听，否则可能在取消时丢失 UI 侧的 `onEnd` 收口。

## 6. 安全与网络

- 联网与模型调用均在主进程或受控链路发起以便统一超时与密钥不落日志（实现上应避免将完整密钥打印到控制台）。
- `webPreferences`（见 `electron/main.ts`）为历史兼容可能存在放宽项；新版本评估时应逐项收紧与安全审计。

## 7. 构建与分发

- **Vite**：打包渲染层；主进程可对 `axios`、`exceljs`、`mammoth` 等拆 **vendor chunk**，降低 `main.js` 解压体积。
- **`vite-plugin-electron`**：联动主进程/预加载；
- **`electron-builder`**：按目标平台产出 DMG/Setup/AppImage；
- CI 可选用 `npm run build` + 各目标 `npm run package:*`。

---

*本文描述与仓库代码一致；迭代后请同步更新需求清单与设计文档章节。*
