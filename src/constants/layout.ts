/**
 * 壳层布局尺寸（px）。
 * ChatComposer 的 tailwind 类 w-[calc(100%-256px)] 为静态字符串（JIT 扫描需要），
 * 与 SIDEBAR_W_PX 保持同步即可。
 */
export const SIDEBAR_W_PX = 256;
export const TITLEBAR_H_PX = 44;
export const FOOTER_H_PX = 76;
